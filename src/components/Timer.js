import React from 'react'

const Timer = (props) => {
    console.log(props.orientation);
    return(
        <div id="timer">
            <h1>Timer</h1>
        </div>
    );
}

export default Timer;